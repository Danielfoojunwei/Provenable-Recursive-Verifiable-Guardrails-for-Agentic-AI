name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build (aegx)
        run: cargo build --locked

      - name: Test (aegx)
        run: cargo test --locked

      - name: Format check (aegx)
        run: cargo fmt --check

      - name: Clippy (aegx)
        run: cargo clippy --locked -- -D warnings

      - name: Build (aer)
        run: cargo build --manifest-path packages/aer/Cargo.toml --locked

      - name: Test (aer)
        run: cargo test --manifest-path packages/aer/Cargo.toml --locked -- --test-threads=1

      - name: Format check (aer)
        run: cargo fmt --manifest-path packages/aer/Cargo.toml --check

      - name: Clippy (aer)
        run: cargo clippy --manifest-path packages/aer/Cargo.toml --locked -- -D warnings
